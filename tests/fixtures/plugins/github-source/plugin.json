{
  "schemaVersion": 1,
  "id": "github-source",
  "name": "GitHub source fixture",
  "version": "1.0.0",
  "author": "symphony-tests",
  "description": "Phase 9A fixture: a canned issue-source plugin (fetch_open_issues + write_back_status, incl. one terminal + one malformed issue) plus get_writeback_log to observe writeback deliveries from a real subprocess.",
  "entrypoint": { "command": "node", "args": ["server.mjs"] },
  "events": [],
  "toolScope": "both",
  "permissions": ["secrets:read", "net:api.github.com"],
  "capabilityFlags": ["requires:secrets-read", "requires:network-egress", "external-visible"],
  "provides": { "issueSource": { "source": "github" } }
}
