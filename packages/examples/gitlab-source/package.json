{
  "name": "@symphony/plugin-gitlab-source-example",
  "version": "0.1.0",
  "private": true,
  "description": "Symphony issue-source plugin for GitLab — pulls issues as tasks and writes terminal status back by adding a note and closing the issue. Repackages the in-tree GitLab connector (Phase 8C.3) as an opt-in plugin via provides.issueSource.",
  "type": "module",
  "files": [
    "dist",
    "plugin.json"
  ],
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "tsup",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "@symphony/plugin-sdk": "workspace:*",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/node": "^20.19.0",
    "tsup": "^8.5.1",
    "typescript": "^6.0.3"
  },
  "license": "MIT"
}
