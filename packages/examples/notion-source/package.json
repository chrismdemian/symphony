{
  "name": "@symphony/plugin-notion-source-example",
  "version": "0.1.0",
  "private": true,
  "description": "Symphony issue-source plugin for Notion — pulls database pages as tasks and writes terminal status back to the page property. Repackages the in-tree Notion connector (Phase 8A) as an opt-in plugin via provides.issueSource.",
  "type": "module",
  "files": [
    "dist",
    "plugin.json"
  ],
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "tsup",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "@symphony/plugin-sdk": "workspace:*",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/node": "^20.19.0",
    "tsup": "^8.5.1",
    "typescript": "^6.0.3"
  },
  "license": "MIT"
}
